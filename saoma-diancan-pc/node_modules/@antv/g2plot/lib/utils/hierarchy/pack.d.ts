import { HierarchyOption } from './types';
declare type Options = Omit<HierarchyOption, 'as'> & {
    as?: [string, string, string];
};
export declare function pack(data: any, options: Options): any[];
export {};
