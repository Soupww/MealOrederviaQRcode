import { Types } from '@antv/g2';
/**
 * 把 padding 转换成统一的数组写法
 * @param padding
 */
export declare function normalPadding(padding: number | number[] | 'auto'): [number, number, number, number];
/**
 * 获取调整的 appendPadding
 */
export declare function getAdjustAppendPadding(padding: Types.ViewAppendPadding, position?: string, append?: number): number[];
